#!/bin/bash

# Too complicated to use NZ's script and the contrast funciton does not
# work because the coefficients are incorrect. 

# Re-designing input so that only two conditions are analysed at a time
